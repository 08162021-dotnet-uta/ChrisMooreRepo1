using Microsoft.EntityFrameworkCore;
using Project1.WebStoreDBContext.Models;
using System;
using System.Linq;

namespace Project1.WebStoreApplication
{
    class Program
    {
        static void Main(string[] args)
        {




            //using(WebStoreApplicationDBContext custcontext = new WebStoreApplicationDBContext())
            //{
             //   var customers = custcontext.Customers.FromSqlRaw("SELECT * FROM CUSTOMERS").ToList();
             //   int count = 0;
             //   foreach(var v in customers)
             //   {
               //     Console.WriteLine($"{++count}-{v.FirstName}{v.LastName}");
              //  }
           // }
            
        
        }
    }
}
